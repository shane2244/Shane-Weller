import Nav from '../components/Nav';

export default function SignInPage() {
  return (
    <main style={{ padding: 24 }}>
      <Nav />
      <h1>Sign in</h1>
      <p>Use the sign in button in the nav to authenticate (NextAuth).</p>
    </main>
  );
}
